{
  "name": "study-tracker-monolid",
  "version": "1.0.0",
  "description": "Root configuration for deploying frontend and backend to Railway together",
  "scripts": {
    "install": "npm install --prefix server && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm start --prefix server"
  }
}
